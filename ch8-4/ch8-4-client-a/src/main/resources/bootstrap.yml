server:
  port: 7070
spring:
  application:
    name: client-a
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.port:127.0.0.1}:${eureka.port:8888}/eureka/
  instance:
    prefer-ip-address: true