server:
  port: 8081
spring:
  application:
    name: client1

eureka:
  client:
    security:
      basic:
        user: admin
        pasword: Xk38CNHigBP5jK75
    service-url:
      defaultZone: http://${eureka.client.security.basic.user}:${eureka.client.security.basic.pasword}@localhost:8761/eureka/
